# 项目行为规范（设计流程）

本项目中，任何设计调整必须严格按以下顺序执行：

1. 先使用 `ui-ux-pro-max` 进行设计判断与视觉把关。
2. 再使用 `gpt-5.3-codex` 完成实现编码。
3. 再使用 `mini` 进行审查与校验。
4. 若结果未达到 `ui-ux-pro-max` 标准，继续迭代，直到达标为止。

额外约束：

- 不得修改用户未明确提出的任何内容。
- 仅在用户明确指定的范围内进行最小改动。
